# Backtrack-Trigger Test — 说明（本案例未重复注入）

`ddd-model-review` 的 5 条回溯触发条件（不变量表达率 < 60%、聚合-上下文边界矛盾、术语冲突率 > 20%、事件完整性 < 70%、集成模式与上下文映射不一致）已在 [Cargo 案例](../cargo-validation/backtrack-test/injection-report.md) 中通过 **5/5 缺陷注入验证**（F1–F5 全部正确激活并路由到预期目标）。

触发机制不随领域变化，本案例（保险承保与理赔，无规范参考）**不重复注入测试**，聚焦验证"无基准时技能体系的真实表现"（见 `REPORT.md` §4）。

若后续需要在本案例上复验，可直接复用 Cargo 案例的注入方法：

| 触发条件 | 建议注入方式（基于本案例工件） |
| :--- | :--- |
| 术语冲突率 > 20% | 把"报案/理赔/赔案"三种命名混用在 04 的不同章节 |
| 集成模式不一致 | 在 05 声明"经 ACL 调外部费率"，但在 07 让 QuoteService 直接透传外部费率原始字段 |
| 不变量表达率 < 60% | 从 06 §不变量表删除大部分条目（保留 I-1/I-4/I-8） |
| 聚合-上下文边界矛盾 | 让 ReserveView 承担赔付金额校验（业务判定越权） |
| 事件完整性 < 70% | 从 07 §事件目录删除 PaymentExecuted / ClaimClosed / FraudInvestigationConcluded 等关键事件 |
