# 08 — ddd-model-review 产出（Blind Run）

> 输入：`04-ddd-contexts.out.md` + `05-ddd-context-map.out.md` + `06-ddd-aggregates.out.md` + `07-ddd-domain-interactions.out.md`（+ `01`~`03` 辅助）。
> 本文件为 `ddd-model-review` 在 blind run 下的模拟执行产出，**未参考**任何真值材料（本案例无参考实现；行业对标维度不启用）。

## 评分总览

| 维度         | 得分 (0-10) | 主要扣分原因                                                                                            |
| :----------- | :---------: | :------------------------------------------------------------------------------------------------------ |
| 术语一致性   |      9      | 词汇表覆盖 7 上下文；TermsSnapshot/TermsVersion 同源概念命名接近但已区分（快照 vs 版本序列），需留意    |
| 边界合理性   |      8      | 7 上下文边界总体清晰；Claim 承载报案受理 + 查勘 + 调查 + 追偿，职责面较大，存在"上帝上下文"苗头         |
| 不变量表达率 |      8      | 10 条显式不变量覆盖主链；巨灾高峰（E7"先收后补"）未固化为显式不变量，依赖基础设施                       |
| 事件完整性   |      9      | 主路径 16 事件 + 11 异常分支均有事件化；`ApplicationQuarantined` 等隔离区事件齐备                       |
| 耦合度       |      8      | Party 主数据被 3 个上下文消费；Claim ↔ Payment 双向事件（指令/回执）为必要耦合；Policy 事件族是最大扇出 |

## 问题清单

| 问题                                                                     | 影响                                 | 证据                                    | 建议修复                                                                                 | 优先级 |
| :----------------------------------------------------------------------- | :----------------------------------- | :-------------------------------------- | :--------------------------------------------------------------------------------------- | :----- |
| Claim 聚合职责面过大（报案/查勘/调查/追偿/结案）                         | 聚合膨胀风险，事务边界可能被拉宽     | 06 §聚合目录 Claim 包含 4 实体 + 8 命令 | 拆分观察：报案受理聚合（ClaimIntake）与理赔处理聚合（Claim）分设，事件流衔接             | 中     |
| 巨灾高峰"先收后补"无显式状态                                             | 高峰场景行为依赖实现纪律，易退化     | 02 §E7 vs 06 §不变量表（无对应不变量）  | 在 Claim 增加"受理中/待补全"显式状态（ClaimPendingInformation 已存在，补充高峰入口语义） | 中     |
| `UnderwritingAcceptanceSpec` 与 `LossPayableSpec` 未在输出中给出判据明细 | 规则可执行性依赖后续细化             | 06 §Specification 模式识别              | 补判据表（输入、规则条目、输出）                                                         | 低     |
| TermsSnapshot 与 TermsVersion 命名相近                                   | 新读者易混淆                         | 04 §词汇表 Policy vs Claim              | 词汇表补一条交叉引用说明（快照=报案时点冻结；版本=保单侧演进序列）                       | 低     |
| 分保结果（ReinsuranceRequested）无闭环事件                               | 分保依赖外部人工确认，决策可能延迟   | 07 §事件目录仅有 ReinsuranceRequested   | 补 ReinsuranceConfirmed / ReinsuranceDeclined 事件                                       | 低     |
| ReserveView 投影与报送共用 Regulatory 上下文                             | 两类消费（内部视图 vs 对外报送）耦合 | 05 §关系矩阵                            | 职责分离为"视图投影"与"报送适配"两个模块（同一上下文内）                                 | 低     |

## 回溯触发清单

| 触发条件                 |           当前值           | 阈值  | 是否触发 | 回溯目标 |
| :----------------------- | :------------------------: | :---: | :------: | :------- |
| 术语冲突率               |            ~5%             | > 20% |    否    | —        |
| 不变量表达率             |   10/10 显式（覆盖主链）   | < 60% |    否    | —        |
| 事件完整性               |  主路径 16/16，异常 11/11  | < 70% |    否    | —        |
| 聚合边界与上下文边界矛盾 | 未发现矛盾（快照边界清晰） | 任一  |    否    | —        |
| 集成模式与映射不一致     |  指令/回执双向有明确契约   | 任一  |    否    | —        |

> 未触发任何回溯；但问题清单中"Claim 职责面过大"建议在下一轮迭代中观察，若继续膨胀应回到 `ddd-contexts` 重划边界。

## 实施就绪判定

**结论：Ready（具备上线可用性）**:

- 主链（投保 → 承保 → 出单 → 理赔 → 支付）事件化完整，不变量覆盖关键业务规则。
- 无阻塞项；"分保闭环事件缺失"与"巨灾显式状态"建议在实施首迭代补齐，不阻塞建模交付。
- 监控指标已定义（决策→出单时延、支付成功率、投影延迟、报送对账差异）。
